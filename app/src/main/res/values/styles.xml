<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="AppTheme" parent="@android:style/Theme.Holo.Light">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:scrollbarSize">4.0dip</item>
        <item name="android:scrollbarThumbHorizontal">@drawable/scrollbar_thumb</item>
        <item name="android:scrollbarThumbVertical">@drawable/scrollbar_thumb</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationStyle</item>
        <item name="android:listSelector">@color/color_00ffffff</item>
    </style>

    <style name="SplashTheme" parent="@style/AppTheme">
        <item name="android:windowBackground">@drawable/windows_background</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="WindowAnimationStyle" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/activity_open_enter_animation</item>
        <item name="android:activityOpenExitAnimation">@anim/activity_open_exit_animation</item>
        <item name="android:activityCloseEnterAnimation">@anim/activity_close_enter_animation</item>
        <item name="android:activityCloseExitAnimation">@anim/activity_close_exit_animation</item>
    </style>

    <style name="slide_popup_ani">
        <item name="android:windowEnterAnimation">@anim/fragment_slide_up</item>
        <item name="android:windowExitAnimation">@anim/fragment_slide_down</item>
    </style>

    <style name="GeneralDialog" parent="android:style/Theme.Holo.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
